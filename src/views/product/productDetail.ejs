<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="/css/productDetail.css">
    <%- include('../partials/linksComunes.ejs') %>
    <title>Fuerza y Disciplina</title>
</head>
   <body>
      <%- include('../partials/header.ejs') %>
    <main>
      <form action="/product/detail/<%= product.id %>" method="POST">
            <div class = 'contenedor'>
                          <div class = 'img'>
                              <img src="/images/products/<%= product.image %>" alt="product image" class = 'img-c' />
                          </div>
                          <div class = 'description-con'>   
                            <h2> Detalle del producto: <%= product.name %> </h2>
                              <p class = 'description'>
                                  <%= product.description %>
                              </p>
                                  <h2> $<%= product.price %> US </h2>
                                <a href="/product/cart"> <input type="submit" value="Agregar al carrito" class = 'b-carrito'></a> 
                                <hr/>
                                <div class = 'botones'>
                                </form>
                                  <a href="/product/edit/<%= product.id %>"> <input type="button" value="Editar el producto" class = 'botoneyd'></a>                                 
                                  <form action="/product/<%= product.id %>?_method=delete" method="POST">
                                      <button class = 'botoneyd' type="submit">
                                          Eliminar
                                      </button>
                                  </form>
                                </div>
                          </div>
              </div>
    </main>
    <footer>
      <%- include('../partials/footer.ejs') %>
    </footer>
   </body>
</html>