<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/css/login.css'>
    <title>fuerza y disiplina </title>
    <%- include('../partials/linksComunes.ejs') %>
    <script src="/js/loginValidation.js" defer></script>
</head>



<body>
     <header>
        <%- include('../partials/header.ejs') %>
    </header>

    <main class="celeste-clarito">
      <h2 class = 'titulo'> Ingresar usuario </h2>

      <div class = "center">

      <form action="/users/login" method="post" class = "input-login product-shadow" >
      <input class="users-login" type="email" id="usuario" name="usuario" placeholder="Email" >
      <% if (errors && errors.usuario && errors.usuario.msg) { %>
        <%= errors.usuario.msg %>
      <% } else if (!errors || !errors.usuario) { %>
        <% if (userError) { %>
          <%= userError %>
        <% } %>
      <% } %>
      <span class="errorMsg" id="usuarioError"></span>
      <br>
      <input class="users-login" type="password" id="contra" name="contra" placeholder= "Contraseña" >
      <% if (errors && errors.contra && errors.contra.msg) { %>
        <%= errors.contra.msg %>
      <% } else if (!errors || !errors.contra) { %>
        <% if (contraError) { %>
          <%= contraError %>
        <% } %>
      <% } %>
      <span class="errorMsg" id="contraError"></span>
      <br>
      <div> <button class = 'inicio-login' type="submit">Iniciar sesión</button> </div>
      </form>
     
      </div>
    </main>
      <footer>
        <%- include('../partials/footer.ejs') %>
        </footer>
</body>
</html>